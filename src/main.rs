// use convert::{decode, Tiles, Word};

// fn calc_scores<W: Word>(word: W) -> u32 {
//     word.tiles().len() as u32
// }

fn main() {
    // let tiles = Tiles::from(vec![1, 2, 3]);
    // println!("{:?}", tiles);
    // let s = decode(tiles);
    // println!("{} {:?}", s, decode(vec![1, 2, 3]));

    // let score = calc_scores("aap");
    // println!("{}", score);

    // let score = calc_scores(tiles);
    // println!("{}", score);
}
